# Data — large CSV not in repo, fetched via `python download.py`
data/raw/*.csv

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
.uv-cache/

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Outputs that should be regenerable, not tracked
# (figures ARE tracked — they're rendered for README)
posterior_samples.npz
